import { FiltersItemsPipe } from './filters-items.pipe';

describe('FiltersItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltersItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
